#Especificar la librería o paquete del cual yo voy a ejecutar una 
#función predterminada
utils::sessionInfo()

#cada vez que aperturamos de cargan por defecto los pauquetes attached base packages

installed.packages()

View(installed.packages())

